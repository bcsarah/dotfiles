general {
    colors = true
    interval = 1
    separator = "  "
}

order += "cpu_usage"
order += "memory"
order += "disk /"
order += "battery all"
order += "volume master"
order += "tztime local"

cpu_usage {
    format = "   %usage "
}

memory {
    format = "   %percentage_used "
}

disk "/" {
    format = "   %free "
}

battery all {
    format = "   %percentage "
}

volume master {
    format = "   %volume "
}

tztime local {
    format = "   %a %d/%m %H:%M "
}
